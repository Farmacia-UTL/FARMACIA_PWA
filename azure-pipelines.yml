# =====================================================
# 📦 Pipeline CI para App Flask (SGT) - Solo Build
# Flujo: CI (Empaquetado) -> Artefacto listo
# =====================================================

trigger:
- main

pool:
  name: 'AgenteLocal'

variables:
  AppSourceDir: '$(Build.SourcesDirectory)/sgt'
  ArtifactName: 'sgt-deploy-package'
  DeploymentPath: '$(Build.ArtifactStagingDirectory)/$(ArtifactName)'
  # ServiceConnectionName y AppServiceName ya no son necesarios sin deploy, 
  # pero los dejo comentados por si los re-activas en el futuro.
  # ServiceConnectionName: 'AZ-Conexion-SGT'
  # AppServiceName: 'sgt-app-integrated-2025'
  PythonVersion: '3.11' # ✅ Versión centralizada

# =====================================================
# 1️⃣ Etapa: CI - Integración Continua (Empaquetado)
# =====================================================
stages:
- stage: CI_Build
  displayName: 'Etapa 1: Integración Continua (Empaquetado)'
  jobs:
  - job: PythonCI
    displayName: 'Job: Instalación y Empaquetado'
    steps:

    # --- 0. Limpiar workspace ---
    - script: |
        echo "🧹 Limpiando directorio de trabajo previo..."
        git clean -xfd
        echo "Verificando contenido actual de requirements.txt:"
        type requirements.txt
      displayName: 'Limpiar workspace y mostrar requirements.txt'

    # --- 1. Configurar versión de Python ---
    - task: UsePythonVersion@0
      displayName: 'Configurar Python $(PythonVersion)'
      inputs:
        versionSpec: '$(PythonVersion)'

    # --- 2. Instalar dependencias ---
    - script: |
        echo "📦 Instalando dependencias..."
        pip install --upgrade pip
        pip install -r requirements.txt || (echo "Reintentando instalación..." && pip install -r requirements.txt)
      displayName: 'Instalar Flask y dependencias'

    # ✅ Crea el directorio de artefactos ANTES de copiar archivos
    - script: |
        # Esto soluciona el error "Not found PathtoPublish"
        mkdir -p "$(DeploymentPath)"
        echo "Directorio de despliegue creado: $(DeploymentPath)"
      displayName: 'Crear Directorio de Artefactos'

    # --- 3. Copiar archivos para despliegue ---
    - task: CopyFiles@2
      displayName: 'Copiar Código de Aplicación'
      inputs:
        SourceFolder: '$(Build.SourcesDirectory)'
        Contents: |
          sgt/** # Tu código fuente, incluyendo main.py
          requirements.txt     # CRÍTICO: Para instalar dependencias en Azure
          templates/** # CRÍTICO: Templates en la raíz
          start.sh # 🛠️ Nuevo script de inicio
        TargetFolder: '$(DeploymentPath)'

    # --- 4. Publicar artefactos ---
    - task: PublishPipelineArtifact@1 
      displayName: 'Publicar Artefacto de Despliegue'
      inputs:
        targetPath: '$(DeploymentPath)' 
        artifact: '$(ArtifactName)'